{
  "name": "irg-gdp-chain-middleware",
  "version": "2.7.0",
  "description": "IRG Chain 888101 transaction submission middleware for IRG_GDP backend. Forwards signed transactions to Besu RPC with retry, audit, and rate-limiting. Never accepts raw private keys over the wire; backend-originated system transactions sign locally using a key loaded from AWS Secrets Manager / KMS.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "dotenv": "^16.4.5",
    "ethers": "^6.11.1",
    "pino": "^8.19.0",
    "express-rate-limit": "^7.2.0"
  },
  "engines": {
    "node": ">=20"
  }
}
